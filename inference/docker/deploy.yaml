apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    run: seamless-manuel-100
  name: seamless-manuel-100
spec:
  replicas: 1 
  selector:
    matchLabels:
      run: seamless-manuel-100
  strategy:
    rollingUpdate:
      maxSurge: 100%
      maxUnavailable: 100%
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        run: seamless-manuel-100
    spec:
      containers:
      - image: seunglab/seamless:jit-100-test-p
        name: seamless-manuel-100
        imagePullPolicy: Always
        command: ["/bin/sh"]
        args: ["-c", "python inference/worker.py --processes 8 --device cpu --queue_name manuel-seamless --lease_seconds 40"]
        resources: 
          limits:
            nvidia.com/gpu: 1
        volumeMounts:
        - name: secrets
          mountPath: /root/.cloudvolume/secrets
          readOnly: true
      dnsPolicy: Default
      volumes:
      - name: secrets
        secret:
          secretName: secrets
status: {}
